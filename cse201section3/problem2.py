# Two sets A and B
A = {1, 2, 3, 4}
B = {3, 4, 5, 6}

# UNION
print(A.union(B))

# INTERSECTION
print(A.intersection(B))

# DIFFERENCE
print(A.difference(B))

#SYMMETRIC DIFFERENCE
print(A.symmetric_difference(B))
